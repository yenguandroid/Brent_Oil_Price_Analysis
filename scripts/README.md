# Scripts

Utility/one-off scripts that support the project but aren't part of the reusable
`src/` package (e.g. data-fetching helpers, one-time notebook generation, report
export helpers). Nothing here is required to reproduce the analysis — that logic
lives in `src/` and is exercised from `notebooks/` and `tests/`.
